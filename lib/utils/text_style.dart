import 'package:flutter/material.dart';
import 'package:practice/utils/colors.dart';

class Textstyles {
  static const headingstyle = TextStyle(color: colors.textcolor, fontSize: 30);
  static const subheadingstyle =
      TextStyle(color: colors.textcolor, fontSize: 20);
}
